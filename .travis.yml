
sudo: required
language: java

jdk:
  - openjdk8

script:
  - mvn clean compile package assembly:single -DskipTests

before_deploy:
    - mkdir MiningBuild
    - find . -name '*.jar' -exec cp {} ./MiningBuild \;
    - cd /home/travis/build/semantic-conflicts/jOOQ/MiningBuild
    - tar -zcvf result.tar.gz *
deploy:
  provider: releases
  api_key: $GITHUB_TOKEN
  file: result.tar.gz
  name: fetchjar-c5b5a73051bf2a9a525eb719d49d7f24aa5df9fb
  file_glob: true
  overwrite: true
  skip_cleanup: true
  on:
    all_branches: true 
            